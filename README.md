# Contact Info Extractor

This Python program scans `example_email.txt` for phone numbers, email addresses, and website URLs using regular expressions. It removes duplicate matches and saves each type of contact information to its own output file: `phone_numbers.txt`, `emails.txt`, and `websites.txt`.